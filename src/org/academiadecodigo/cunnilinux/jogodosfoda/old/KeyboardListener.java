package org.academiadecodigo.cunnilinux.jogodosfoda.old;

import org.academiadecodigo.simplegraphics.keyboard.Keyboard;
import org.academiadecodigo.simplegraphics.keyboard.KeyboardEvent;
import org.academiadecodigo.simplegraphics.keyboard.KeyboardEventType;
import org.academiadecodigo.simplegraphics.pictures.Picture;
/*
public class KeyboardListener {
    Keyboard keyboard;


    private Picture heroDireita;
    private Picture heroEsquerda;

    public KeyboardListener(Picture heroDireita, Picture heroEsquerda) {
        this.heroDireita = heroDireita;
        this.heroEsquerda = heroEsquerda;
        this.keyboard = new Keyboard(new MyKeyboardHandler(heroDireita, heroEsquerda));
    }

    public void addEventListener( int[] keysArray) {

        for (int key : keysArray) {
            keyboard.addEventListener(createKeyboardEvent(key, KeyboardEventType.KEY_PRESSED));
        }
    }

    /*
     *  Create a keyboard event

    public KeyboardEvent createKeyboardEvent(int keyboardEventKey, KeyboardEventType keyboardEventType) {
        KeyboardEvent keyboardEvent = new KeyboardEvent();
        keyboardEvent.setKey(keyboardEventKey);
        keyboardEvent.setKeyboardEventType(keyboardEventType);
        return keyboardEvent;
    }
}

*/
